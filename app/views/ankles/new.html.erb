<%= form_for(@ankle) do |f| %>

　　<%= f.label :id %>
　　<%= f.text_field :id %>

　　<%= f.label :image %>
　　<%= f.file_field :image %> （画像用に追加したカラムの名前）

    <br><%= f.submit %>
<% end %>


<%= link_to 'Back', ankles_path %>
